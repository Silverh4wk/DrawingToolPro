package src;

public class AnimalCreation extends CreationType { 
    // Constructor for AnimalCreation that takes a name and an image path
    public AnimalCreation(String name, String imagePath) {
        super(name, imagePath); // Call the constructor of the superclass (CreationType)
    }
}
